{
  "name": "mipuerta-app",
  "version": "1.0.0",
  "description": "Sistema de puerta con videollamada",
  "scripts": {
    "build": "npm run build-web && npx cap sync",
    "build-web": "echo 'No hay bundler, usando archivos crudos en www...'",
    "start": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/push-notifications": "^6.0.0",
    "@capacitor/toast": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
